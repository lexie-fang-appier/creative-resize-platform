services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: creative_resize_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - creative_resize_pgdata:/var/lib/postgresql/data

volumes:
  creative_resize_pgdata:
